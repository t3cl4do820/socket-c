# ! This is a project for school !

### Introduction
Its a simple chat server made in C language, basically all the messages will be saved in a `logs.db` file, so a `log_monitor.c` file will search for n banned words in`logs.db` and if a user like "chad123" has three points (one point per banned word used) the user will be blocked from the chat.
